header
footer
main > *
  width: 100vw
  +above($pageWidth + 1)
    padding-left: 'calc((100% - %s) / 2)' % $pageWidth
    padding-right: 'calc((100% - %s) / 2)' % $pageWidth
  +below($pageWidth)
    padding-left 1rem
    padding-right 1rem

main > *
  position: relative
  min-height: $sectionMinHeight

main
  display: table
  width: 100vw
  overflow: hidden

[tabindex]
  outline: none
